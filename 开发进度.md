# BufferC 开发进度

> 最后更新：2026-08-17（复盘 B3/B4/B5 落地：历史持久化、重启全部重执行+防误清校验、对账严格开关）

## 2026-08-17（续 3）：复盘 B3/B4/B5 落地 + A8 修复（用户逐项拍板）

- **B3 历史持久化**：Inventory 新增 history_events/history_commands/history_alarms/history_failed 四表（每表保留最近 2000 条自动清理）；BufferCService 写通（事件/命令/告警/悬空命令）+ 启动 SeedHistoryFromDb 回填环形缓冲——Web 面板重启后可查历史
- **B4 重启恢复改「全部重执行 + 防误清」（用户确认）**：新增 cmd_state=4 恢复待校验——重启把 1/2 → 4（不再直接 9001）；worker 校验「站口当前载具 ID == 命令载具 ID」：一致（或站口已空）→ 转 1 正常执行；不一致（停机期间货被换过）→ 标记失败 + 9001 人工介入。安装幂等、取走防误清
- **B5 对账严格模式开关**：config `reconcileStrict`（默认 false 保守只记差异；true 删除 MCS 未包含的本地条目）；config.json×2 与发布脚本模板同步
- **A8 修复**：①S1F3 解析失败回空 S1F4（不再让 MCS 干等 T3）②S6F11 发送失败补上下文 WARN（SendEvent 辅助收编 13 处 + HsmsServer 2 处）
- **现场确认回填**（§5 台账闭环）：S2F33/35/37 MCS 不使用（维持无配置直接推送）、载具 ID ≤32、input/outputRequest 不发送（代码已移除，测试锁定）、BCR NG 流程取消（删 Scenario10）
- **验证**：106/106 全绿（InventoryTests 状态机更新为 4 态 + 历史持久化跨重启测试）；0 警告；文档同步（手册 §1 reconcileStrict/§7 已知限制 4 条）

## 2026-08-17（续 2）：现场确认项回填（用户拍板，§5 台账闭环）

## 2026-08-17（续 2）：现场确认项回填（用户拍板，§5 台账闭环）

- **S2F33/35/37 事件报告配置：MCS 不使用** → 维持「无配置直接推送」模式，不回 S9F5 问题不成立（记录进手册 §7）
- **载具 ID ≤32 字符**（现场确认）→ 现有 ID 区 16 寄存器设计够用，B2 关闭
- **input/outputRequest 不发送**：代码已按此实现（AgvcClient 载荷无此两字段，ScenarioTests:1361-1363 有断言锁定）——本次仅回填确认
- **BCR NG 流程取消**：删除 Scenario10_BCR_NG 测试；PlcPoller 的 UNK 前缀检测保留（防御性无害）；手册 §7 记录「501 仅正常扫码路径」

## 2026-08-17（续）：项目复盘 A1-A7 缺陷修复（逐项经用户确认后实施）

## 2026-08-17（续）：项目复盘 A1-A7 缺陷修复（逐项经用户确认后实施）

全项目复盘（代码+文档+部署调研）产出 A~E 清单，A1-A7 本次修复：

- **A1 T3Ms 死配置**：HsmsServer SendRaw 硬编码 45s → 改用 `_cfg.T3Ms`（改 config 真正生效）
- **A2 T3 监控竞态**：`_outstanding` 三线程访问加 `_outstandingLock` 统一串行化；T3MonitorAsync 加 try/catch 兜底（绝不让监控任务静默死亡）+ 收集超时后锁外打日志
- **A3 事件引擎加锁**：EventEngine 内部 `_lock` 串行化 15 线程并发 Diff/Reset/MarkScanInstalled（推导极轻，开销可忽略）
- **A4 快照发布深拷贝**：PlcSnapshot 新增 Clone()（EventEngine 原私有 Clone 收编），poller `_snapCopy = _snap.Clone()`——消除 UI/SVID/推送线程读半轮混合状态
- **A5 cmd_seq 回写**：Inventory 新增 WriteCommandSeq；命令失败分支回写 `Channel.LastSeq`（失败命令也能对 400 触发编号）
- **A6 9001 自动清除（口径用户确认 2026-08-17）**：下条命令成功 → AlarmClear(9001) + S5F1 CLEAR + 告警历史 CLEAR 记录 + 审计（与 PLC 告警「恢复即消」语义一致；失败事实留痕审计/台账）
- **A7 发布 config 自动化**：新增 `deploy/制作现场linux包.sh`（发布 → 覆盖现场模板 config：baseUrl 空/15 台 PLC 占位/webPort 7001/logLevel info → 打 tgz）——08-14/08-17 各踩一次「忘改 baseUrl 带到现场」，人工规程靠不住；部署说明 §1/§2 改指向脚本、§4 防火墙 8080→7001
- **验证**：**106/106 全绿**（新增 FixRegressionTests 4 个：T3Ms 生效+监控存活×2 次超时、快照 Clone 独立性、失败 seq 回写、9001 SET→成功自动 CLEAR 全链路）；命令挂起/断线重连场景脚本 PASS；linux 包重发（tgz 41M，config 已为现场模板）

## 2026-08-17：日志梳理与优化（五档级别 + 按天滚动 + audit 审计日志）

## 2026-08-17：日志梳理与优化（五档级别 + 按天滚动 + audit 审计日志）

用户四项决策：①按天滚动保留 7 天 ②引入 WARN/ERROR 写入日志行 ③帧日志默认保持开 + 关闭开关省掉 HEX/SML 转换 ④SECS 收发摘要+命令生命周期单独进 audit.log。

- **新行格式**：`[2026-08-17 09:30:00.123][INFO ][PLC1][#000042] 消息`——补日期/级别（定宽5）/seq（普通与审计共用计数器，两文件互相关联）；入口 OneLine 折叠多行（帧日志 SML 也单行化），一行一条
- **五档级别**：LogLevel Error=0/Warn=1/Info=2/Debug=3/Trace=4（升序，两处过滤比较不用动）；异常路径升 ERROR（连接异常/悬空命令/命令失败/S5F1 失败/SQLite 写失败）、重连退避/解析失败/未连接发送/T3 升 WARN；Validate 白名单五档；前端级别按钮加 Error/Warn + 着色
- **按天滚动**：`bufferc-YYYYMMDD.log`（基名 bufferc.log 扩展名前插日期）；每行写前在 _logLock 内比日期翻转 → 切新文件 + 清理过期；保留 7 天（logRetentionDays 配置 1~365）；BOM 只在新建/空文件写一次（同日重启 append 不再写中间）；Clock 可注入（测试拨日期）
- **审计通道**：`Audit(category,msg)` 独立方法——控制台 + `bufferc.audit-YYYYMMDD.log` + 环形（Web 可见），不写主日志不过级别过滤；auditFile 置空整体关闭。接线：HsmsServer 新增 Audit 事件（收发摘要 `SxFy sys=N` 常开不受 logFrames 约束 + S2F41 参数 + T3 超时）；BufferCService（悬空命令/9001/S5F1 SET|CLEAR、命令入队→执行→完成 seq=LastSeq、重启恢复、CIM、对账 789/794/795/803/806 全迁 Audit）；量级 ~14MB/天
- **帧日志省转换**：LogFramesEnabled=false 时 Traffic 环形仍记条目但 Hex/Sml 置空（跳过 ToHexString/Sml，CPU 热点消除）；S2F41 明细从 LogFrame 改 Audit
- **旁路收编**（4 处裸 Console + ASP.NET 第三种格式）：CommandChannel 构造注入 log（category 逐字符 `Cmd` 保 `[Cmd]` 锚点；首试/成功 Debug、重试 Warn、最终失败 Error——默认 info 下 [Cmd] 只出现在重试/失败，保回显丢失.sh 断言力）；Inventory 构造注入（默认 Console 回退，单测不破）+ SQLite 写失败节流（首条 ERROR、每 10 次/60s Debug、恢复 Info）；WebUi `ClearProviders()` + `[Web]` 收编；Program Ctrl+C 收编（44 行保留裸 Console：Stop 已 dispose writer）
- **排障提升**：CMD 消息补 PLC 编号；LogException 辅助（Message 行 + Debug 堆栈行）；断线节流（streak 首条 ERROR 含原文、重复降 Debug 带计数）；已连接带第 N 次连接；CommandChannel 回显等待不再静默（开始/传输异常/成功/失败全记录）；T3 假文案「→ S9F9」修正为「仅记录不发送」（B2 现状不改行为）
- **兼容性**：场景脚本 4 个零改动（`[Cmd]`/9001/S5F1/ip:port/异常原文锚点逐条核对保住）；Scenario28 唯一必破——改为先关帧日志再建连 + `HEX=` 标记（audit 摘要行无 HEX）；Scenario26/27/29 不破；模拟器不动
- **验证**：**102/102 全绿**（新增 LoggingTests 5 + RollingLogTests 3 + BypassLoggingTests 2）；新行格式/折叠/BOM/五档过滤/滚动切文件/同日 append/7 天清理/Audit 三路/收编委托均有单测锁定

## 2026-08-14（续 9）：AGVC 联调页重做（手动触发 + 发送记录）

- **页面全部推倒重做**（删掉模拟信号/命令流程旧卡片）：只保留两块——①**接口手动触发**：queryMachines（填 cmsIndex → 发送）与 pushDeviceStatusInfo（cmsIndex + service/present/trayId 可选，留空=按 PLC 当前状态推导，与自动推送同口径）；②**发送记录**：手动+自动全量（/api/agvc/traffic 表格 + 统计行「queryMachines 成功N/失败N；push 成功N/失败N」+ 2s 轮询）
- **后端新增**：`POST /api/agvc/manual/queryMachines`（与自动路径一致：查 ID→写 PLC+台账+201）、`POST /api/agvc/manual/pushDeviceStatusInfo`（字段留空按实况推导）；手动调用同样入 traffic 记录（onTraffic 统一回调）
- **坑**：net6 minimal API 的 **async 处理器**在 await 之后响应已开始，Json 助手设 StatusCode 会抛「Headers are read-only, response has already started」——手动触发端点沿用全站同步处理器风格（阻塞式 GetAwaiter().GetResult()，与 ManualInstall 等命令路径一致）
- **验证**：0 警告 + **70/70 全绿**（新增 Scenario40：手动 query→写 PLC/手动 push→traffic 记录/非法 cmsIndex）；冒烟：手动 query 回「已获取货物 ID AGV001 并写入 PLC」、手动 push 按实况推导、记录区两条记录；双平台重发布 + WSL 同步
- 插曲：编译反复「瞬时错误」真凶=**VS 里调试运行的 BufferC.Host 锁住 bin DLL**（MSB3026 文件被锁），停止 VS 运行后即正常——今后编译失败先查 VS 运行实例（教训①第 8 次）
- **⚠️ 发布坑（新增）**：src config 的 `agvc.baseUrl` 为本地联调改成了 `http://127.0.0.1:5502`（VS 调试用）——**发布 linux 包会继承它**，必须发布后把 `publish/linux-x64/config.json` 的 baseUrl 改回空再打 tgz（现场包保持出站禁用，部署时现场填真地址）；win-x64 包保持 5502 供本地联调。已按此操作并校验三份 config 各自正确

## 2026-08-14（续 8）：新页签「PLC 单机测试」（命令协议测试，AGVC 页不动）

- **新页签**（nav「命令」后）：只做命令协议测试——PLC/站口手填，**②③ 地址放开手填、④ 编号手填**（零自动计算）；字节序固定=config.json（lastStatus 每 1.5s 同步 /api/status，config 改显示与打包自动跟随）
- **回显/握手 306~340 自动刷新区**（复用 PLC 详情页数据源；目标站口行黄色高亮、④成功后绿色标粗）
- **下发记录**（富文本，每步 2~4 行）：主行时间+结果 OK/失败+耗时 → 转译行（①清旧命令/②命令码语义/③PLC 收到 400 后拷入存储区/④PLC 只看 400 新值扫描执行）→ 原始数据行（地址+十进制+0x 十六进制）→ 回读/回显行
- **8 项观察增强**：④ 后每 200ms 等回显最多 3s 记耗时；每步耗时；④ 旁实时显示当前 400 值（防填旧值不触发）；命令码 1/2 快捷填充+联动校验；回显区高亮+标绿；④ 成功后自动读存储区验证「PLC 已处理」（回显证收到、存储区证处理）；失败诊断清单；输入框回车执行
- AGVC 联调页完全不动；后端/测试零改动（69/69 全绿）
- 教训：**Host 静态文件根 = 进程 CWD**——冒烟从仓库根启动会 404（Content root 日志可见）；从 publish/win-x64 启动即正常（现场部署同此）

## 2026-08-14（续 7）：前端联调测试台（AGVC 联调页 + Simulator agvc 假服务器）

**目标**：通过前端页面触发模拟的 PLC 状态变化，闭环验证 PLC/AGVC/MCS 业务收发。
- **后端**：AGVC 出站交互记录——AgvcClient 两方法末尾经 onTraffic 回调写入环形缓冲（类型/cmsIndex/请求体/最终响应/成败，100 条）；`GET /api/agvc/traffic` 端点（WebUi）
- **前端第 6 页签「AGVC 联调」**：①模拟触发区（单步写寄存器，复用 /api/debug/regwrite，每次写后自动追读回显）：状态区 0/1/2/3/5、控制状态 0/1、扫码 323/324~339（前端按字节序打包 16 字）/340、ID 区写/清（50+16×(st-1)）、命令区 400/401+st-1/整段清 401~672（272 字）；站口快照对照（/api/status）②AGVC 交互面板（本页 2s 轮询 traffic，请求/响应/成败全展示）
- **Simulator agvc 模式**：`BufferC.Simulator agvc [--agvc-port 5502] [--carrier-id AGV001]`——HttpListener 假 AGVC：queryMachines 按配置应答 carrierId（空=无结果）、pushDeviceStatusInfo 记录；**GET / 自带自刷新网页**展示收到的全部请求（交互全前端化）；q 退出
- **验证**：0 警告 + **69/69 全绿**（新增 Scenario39 traffic 端点断言）；端到端冒烟（agvc 5502 + plc 5501 + Host 8099）：状态 2→1 → traffic 面板 queryMachines（响应带 AGV001）+ push×2 → AGVC 假服务器网页收到 3 条 → PLC ID 区写入 AGV001 ✓；win-x64/linux-x64/Simulator 已重发布，tgz 与 WSL 已同步
- 教训：①pkill -f 会匹配 sh -c 自身命令行（cmdline 里含同串文本），WSL 脚本里要么用隔离的 pattern 要么干脆不 pkill（tar 覆盖不受运行实例影响）②Simulator agvc 的 seq 计数用 `long[1]` 数组（async 方法不能传 ref 参数）

## 2026-08-14（续 6）：寄存器读取修复（FC03 分段 + 前端降级）

- **后端**：ModbusTcpClient.ReadHoldingRegisters 补 FC03 ≤125 自动分段（此前只给 FC16 做了——命令区 273 字/ID 区 256 字单帧超限被真 PLC 拒收）；ReadPlcRegs 上限 125→512（客户端自动分帧）
- **前端**：ID 区/命令区读取失败时自动**降级 16 字小片**重读（轮询器读 ID 区同尺寸，现场可用），彻底兼容真 PLC 帧长限制；降级仍失败显示「已尝试 16 字降级」→ 可区分「帧长问题」与「写区禁止 FC03 读」
- 待现场确认：命令区 400~672 真 PLC 是否禁止读（写区）；站口1 ID 区为空疑似 PLC 侧本就无数据（非 bug）
- **验证**：0 警告 + 68/68 全绿；本地 PlcSim 全链路复现 6 分段全通

## 2026-08-14（续 5）：AGVC 新增出站 pushDeviceStatusInfo（变化即推）

- **接口**：BufferC 主动调 AGVC `POST {baseUrl}/api/hsmsRpc/pushDeviceStatusInfo`，body `{status:[{cmsIndex, service, present, inputRequest, outputRequest, trayId}]}`（单站口一条）；成功判定 code=="0"，失败重试 retryCount 次仅日志（与 queryMachines 同模式）
- **字段语义（现场确认）**：`service` = PLC 34~49 控制状态**原始寄存器值**（0=在服务/1=停用）；`present` = 状态区 1/5 有货；`inputRequest`/`outputRequest` **恒 "0"**；`trayId` = 有货时取事件已知 ID → 台账 → 快照（快照 ID 仅状态变化时重读，扫码/命令写入后陈旧），**无货恒空**
- **触发=变化即推**：HandleEvent 的 204/203/201/501/302/301 事件 → PushStationStatus；**ManualInstall 成功分支也要直接推**（命令路径发 201 不经过 HandleEvent——Scenario32 抓出来的）；离线不推（快照陈旧）
- AGVC 出站接口现共 2 个：queryMachines（到货要 ID）+ pushDeviceStatusInfo（状态推送）
- **验证**：0 警告 + **68/68 全绿**（新增 Scenario38：0→5/扫码/控制状态/取走 4 种变化 → 5 条推送断言 service/present/trayId/恒 0 字段；Scenario32/33/34 请求断言按 path 过滤适配混合推送）

## 2026-08-14（续 4）：AGVC 入站接口移除

- **两个入站接口按现场要求移除**：`POST /api/agvc/delivery`（送达通知）与 `POST /api/station/updateServiceStatus`（站口服务状态）——含 BufferCService.UpdateStationServiceStatus / AgvcServiceUpdate / WebUi.UpdateServiceStatusReq 全部删除；站口服务状态后续由 PLC 侧 34~49 寄存器与事件引擎 301/302 自然覆盖（poller 读回仍会上报）
- AGVC 集成收敛为**仅出站**：`POST {agvc.baseUrl}/api/hsmsRpc/queryMachines`（2→1 到货无 ID → 查货物 ID → 自行写 PLC + 台账 + 201）
- 场景 25 改用 /api/command 验证同一命令路径与悬空记录（C1）；Scenario35/36 删除；agvc 配置段与校验不变（字段全部被出站方向使用）
- **验证**：0 警告 + **67/67 测试全绿**（69-2）

## 2026-08-14（续 3）：状态区新增 5（人工有货）+ AGVC 接口 v2（queryMachines）

**A. 状态区 2~17 新增 5=（人工）有货已存储**（PLC 侧协议变更，现场确认）：
- **入货判到达值**：2→1 = AGV 放入（报 204 + 触发 AGVC 要 ID）；0→5 = 人工放入（报 201，ID 后写可能为空），扫码路径照旧 501+201 补 ID
- **取货判路径**：经 3→0 = AGV 取走（203 HandoffType=AGV）；1/5→0 直跳 = 人工取走（203 HandoffType=MANUAL）——货的存放方式不影响取货方式判定（人工存可 AGV 取、AGV 存可人工取）
- **异常跳变 1→5 / 5→1 只记日志不上报**（货未动换类型标记；EventEngine.OnAbnormalTransition 回调）；状态 4 保持现状；出货删 PLC ID 维持 MCS 命令触发
- 台账/SVID/Web：状态 5 与 1 同为「有货」（Carriers 合并、UI 标签「人工有货」、有货计数含 5）
- **顺手修缺陷**：ManualInstall 台账/201 事件 loc 统一用标准格式（输入兼容 "10001" 纯数字 → 存 "Buffer1_Port1"）——此前存原始 loc 导致 Carriers 快照兜底按标准 loc 找不到台账条目而重复补一条空 ID 记录

**B. AGVC 接口规格 v2（现场新 spec）**：
- 出站 URL 改 `/api/hsmsRpc/queryMachines`（原 pushDeviceStatusInfo 废弃）；请求 `{cmsIndex:"10001"}`；响应 `{code, message, reqCode, interrupt, data:[{carrierId,...}]}`——**carrierId 直接取自响应，BufferC 拿到后自己写 PLC（命令1）+ 台账 + 201**，不再等 AGVC 回调 delivery
- 触发链路不变（2→1 + arrivalGraceMs 等待窗口 + 到期复检 PLC 在线/有货/ID 空）；失败重试仅日志不告警（code≠0/空 data/网络失败均可重试，最终失败站口保持无 ID）
- AgvcClient.PushArrivalAsync → QueryCarrierIdAsync；方向2 updateServiceStatus、delivery 端点、cmsIndex 规则不动

**验证**：0 警告 + **69/69 全绿**（Scenario32/33/34 改造适配新接口含空 data 重试变体；新增 Scenario37 覆盖 0→5 201 空 ID/扫码补 ID/5→0 MANUAL/5→3→0 AGV/异常跳变零上报；Scenario12 按新规则 1→0 直跳改断言 MANUAL；EventStorm 死线 10s→15s——全套并行高负载下偶发不够，单跑 3 连过确认非逻辑回归）

## 2026-08-14（续 2）：PLC 命令协议改造——发命令前整段清零 401~672

PLC 侧商议确认的协议规则（**08-13 待议项已解决**）：
- **PLC 行为**：仅 400 出现新值时才扫描 401 以上寄存器数据执行（400 是唯一扳机）
- **BufferC 写序**：① FC16 整段清零 401~672（命令区+货物ID区，清掉旧命令/旧 ID，防非幂等重复执行）→ ② 写【各站操作命令】→ ③ 写【货物ID写入】→ ④ 最后写 400（新编号）触发（写序按现场确认：命令码在前、ID 在后）
- **FC16 自动分段**：ModbusTcpClient.WriteMultipleRegisters 按规范单帧 ≤123 字自动分帧（272 字 = 3 段），顺带修复 /api/debug/regwrite 大批量写真 PLC 会拒收的隐患
- 往返代价：CmdWrite 6 次/CmdClear 5 次（原 3/2 次），局域网毫秒级对 5s 超时无感
- **失败处理（回显为唯一成功标准）**：Execute 重试只捕 ModbusException → 扩为 ModbusException+IOException；区域未就位（清零/写内容中途失败）→ 重试完整重写；区域已就位（400 响应丢失/回显丢失）→ 重试**只重写 400 新编号**再触发（PLC 靠 400 新值触发，同一条幂等命令重复执行无害）；TCP 无法区分「400 没到」与「到了没响应」（两军问题），判定靠回显寄存器 306~322 核对，不看 400 写响应本身
- **验证**：0 警告 + N/N 测试全绿（ModbusStackTests +5：272 字分段生效 / 预埋脏数据全区域扫描 / 回显丢失重试仅重写 400 成功 / 清零中途写失败完整重写成功 / 400 写失败仅重写 400 成功；PlcSim 新增 FailNextWrites 写失败注入）
- 教训：协议写序用户与 PLC 现场确认过——「先清零→再写→再触发」与「一次成形」在 400 扳机语义下等价，按用户选定字面顺序实施；写失败注入按写请求序号定位（CmdWrite 序列共 6 次写，CmdClear 共 5 次），数错序号测试会注入到错误的写

## 2026-08-14（续）：AGVC 接口冻结，转 PLC 处理逻辑

- **AGVC 接口与接口调用规则暂停改动**（代码保持现状不再动：出站 pushDeviceStatusInfo 触发链路 / 入站 updateServiceStatus / delivery 兼容 cmsIndex 均按已实现状态保留，现场联调前不调整）
- 当前工作转向 **PLC 处理逻辑改造**（见上节：命令协议清零规则）

## 2026-08-14：AGVC HTTP 集成（pushDeviceStatusInfo 出站 + updateServiceStatus 入站）

**方向1（出站，BufferC 调 AGVC 要货物 ID）**：
- **触发**：站口状态 2→1（AGV 放入完成，人工 0→1 直跳不触发）→ 起等待窗口（`agvc.arrivalGraceMs` 默认 3000ms）→ 窗口内 340 扫码握手到达则取消（扫码路径接管）；到期复检 PLC 在线 + 仍为有货 + ID 区空 → POST AGVC `/api/hsmsRpc/pushDeviceStatusInfo`，body `{status:[{cmsIndex, service:"1", present:"1", inputRequest/outputRequest:"0", trayId:""}]}`（trayId 空=索取中）
- **ID 回传**：AGVC 回调已有 `/api/agvc/delivery` → 写 PLC（命令1）+ 台账 + S6F11(201)；**该端点 loc 解析扩展兼容 cmsIndex 纯数字格式**（"10001" 与 "Buffer1_Port3" 均可）
- **失败处理**：重试 `agvc.retryCount` 次（默认 3）+ 仅日志不告警（用户确认）；最终失败该站口保持无 ID，现场 Web 人工补
- **cmsIndex 规则**（现场确认）：`机台号×agvc.cmsIndexBase(10000)+站口`，10001=1号机台1号站口…150016=15号机台16号站口
- 新类 `Core/AgvcClient.cs`（src/ 首个出站 HttpClient，BaseUrl+Timeout 来自配置）；EventEngine 加 `OnAgvPutCompleted` 回调（不新增事件种类，避免内部事件泄漏到 MCS/UI 事件流）

**方向2（入站，AGVC 调 BufferC）**：
- `POST /api/station/updateServiceStatus`：`{stationList:[{cmsIndex, stationStatus: inservice|outservice, updateTime?}]}` → 逐项写 PLC **34~49 站台控制寄存器**（outservice=1/inservice=0），poller 读回后自动报 302/301 给 MCS；HTTP 恒 200、JSON `code "0"/"1"` 携带结果；逐项处理失败聚合进 message；updateTime 仅日志（InvariantGlobalization → TryParseExact 显式格式）
- 配置 `agvc` 段：baseUrl（空=出站禁用）/timeoutSec/retryCount/retryIntervalMs/cmsIndexBase/arrivalGraceMs，均带 Validate 校验

**验证**：0 警告（-warnaserror）+ **63/63 测试全绿**（新增 Scenario32~36：出站触发与字段断言/delivery 回传 cmsIndex/扫码窗口取消/应答重试 3 次/不可达仅日志不告警/服务状态写寄存器+302/301/混合批部分成功/离线项失败/幂等；ConfigValidation +2 例）
- 教训：①xUnit filter 的 `[2-6]` 字符类不生效，多场景过滤要逐个 `~Scenario3X|~Scenario3Y` 列出 ②新 AGVC 场景全部用独立 PlcSim（不复用类级 fixture 的 _plc）——扫码握手 340 会被 fixture 服务的轮询器竞态应答，两个服务同时写命令区会互相搅乱回显 ③Action 委托没有可选参数，包装 Log 方法时 LogLevel 要显式传

**待办**：现场联调前把 AGVC 实际 baseUrl 填入 config.json 的 agvc 段；接口1 input/outputRequest 两字段暂恒 "0"（语义待现场定，代码已留位）

## 2026-08-13（续 3）：调试面板按现场反馈精简

- **PLC 调试面板精简**：删掉「读寄存器」排（与 PLC 详情页寄存器视图重复）；面板改为「命令直发 / 寄存器直写」——下发命令（PLC/站口/命令码/载具ID 全部手填输入）+ 写寄存器（如手动回写 340=0）；执行后结果区仍自动追读 306~322 回显
- **文案修正**：写/读的"起始地址+数量"改为「起始 X 共 N 字」（原 `400+1` 像算式被现场误读）
- 测试记录文档 §1/§2 同步（查看寄存器走 PLC 详情页；读寄存器条目移除）
- 验证：JS 语法检查 + 0 警告 + 56/56 连跑 2 遍全绿（中间一次 2 挂为瞬时并发 flake，复跑即绿）；双平台重发布，WSL 已同步
- **待议（PLC 侧确认后定）**：401~416 旧命令寄存器不清理 → 若 PLC 程序非幂等存在重复执行风险；方案=命令路径②改为整段 FC16 写 401~416（其余补 0）

## 2026-08-13（续 2）：状态区地址序 + 命令码放开 + 位置格式放宽

现场继续反馈三项：
- **状态区 0~49 严格按地址 0→49 从上到下排**（原来按「站口×三类」循环排：0、1、2、18、34、3…）
- **PLC 调试命令码放开手填**（0~65535 任意，不再固定 1/2 下拉；后端 /api/debug/cmd 只保留站口校验；仅 1 会写 ID 区）
- **手动命令位置格式放宽**：正则改为取前两个数字（支持 Buffer1_Port3 / 1号Buffer_3号站口 / Buffer1-3 等）；新增站口越界校验（1~16，原来 Port99 会写进 ID 写入区）+ int.TryParse 防超大数字 500；AGVC 接口同路径自动受益
- 回归：Scenario31 扩展（cmd=9 直发成功回显 / 宽松位置 install 成功 / 站口越界 400 / 单数字位置 400）；56/56 全绿；win-x64/linux-x64 重发布，tgz 与 WSL 已同步
- 插曲：测试 29 挂 = WSL 里用户自启的 BufferC 占 5000 端口（教训①又踩一次，wslrelay 把 WSL 端口转发到 Windows 侧）；用户停掉后 56/56 全绿

## 2026-08-13（续）：Web 前端 5 页重构 + 寄存器视图修复补全

现场反馈的两个 UI 问题（方案与用户逐项确认）：
- **5 页分页**：顶部导航 总览（15 台紧凑表，一屏看完）/ PLC 详情（单台 16 站口 + 完整寄存器视图）/ 命令（手动命令 + PLC 调试）/ 事件告警 / 日志；**轮询瘦身**——status 常开（状态栏+当前页共用），活动/日志仅激活页轮询；总览与详情页**原地更新单元格**不重建 DOM
- **寄存器视图**：①展开被 1.5s 全量重绘"缩回"→ 改为常开 4 栏分区（状态区 0~49 / 回显握手 306~340 自动刷新；ID 区 50~305 快照 ASCII + 按钮读原始 HEX；命令区 400~672 按钮按需读取，真 PLC 不映射时优雅失败）②补全 50~305 与 400~672（经 /api/debug/regread 分段 FC03×3）；客户端含与 RegisterMap.UnpackAscii 一致的解包
- **验证**：0 警告 + 56/56 测试（后端零改动）；JS 语法 node --check；win-x64 自包含实跑冒烟（5 页导航/API/离线优雅失败）；win-x64、linux-x64 均已重发布，tgz 已更新
- 教训：命令区"按需读取"的值不能被快照轮询的原地更新覆盖（data-r 键过滤，仅更新快照键）

## 2026-08-13：现场部署里程碑 + PLC 单独调试工具

- **🎉 现场部署成功**：程序已部署到现场服务器（CentOS 7.8）并启动运行，已连上其中 1 台 PLC（Modbus TCP 502），成功读到寄存器内容
- **新增《PLC交互功能测试记录.md》**（deploy/）：12 项寄存器级测试细目（连接/读寄存器/命令下发 1&2/编号自增/字节序/unit_id/超时重试/扫码握手 340/断线重连/告警寄存器/15 台并发），含 步骤/预期/日志关键字/结果/确认人 栏；测试项 1-2 已预填 ✅
- **新增 Web「PLC 调试」面板**：命令直发（选 PLC→站口→命令码 1=写入ID/2=清除→载具ID→二次确认）+ 寄存器直读/直写（二次确认）+ 结果区；直走 CommandChannel.Execute（含回显等待/重试），**绕过业务副作用**（不动台账、不发 MCS 事件），用于单独与 PLC 调协议。API：`/api/debug/regread|regwrite|cmd`（PlcPoller.ReadRegs/WriteReg(s)、BufferCService.DebugSendCmd）；回归 Scenario31
- **顺手修缺陷**：命令耗时亚毫秒截断为 0（本机回环仿真下 ElapsedMilliseconds=0），与「无命令」语义混淆 → Scenario29 全量并发下必现失败 → CommandChannel 记录下限 1ms；Scenario29 断言改 JSON 解析（子串匹配误判 count=2~9）+ 死线 10s→15s
- **验证**：0 警告编译 + **56/56 测试全绿（连跑 2 遍）**；linux-x64 已重新发布，`bufferc-linux-x64.tgz` 已更新（含新 wwwroot）
- **待办**：现场部署新包 → 按测试记录文档逐项过命令下发；其余 14 台 PLC 接入；MCS 侧联调；现场确认项回填开发文档 §5

## 2026-08-11（续）：调试三方向设计落地

**A. BufferC 调试功能**：日志分级（info/debug/trace，config `logLevel` + Web 运行时切换）、Modbus 帧日志（`logModbusFrames`，trace 级）、出站 HSMS 帧补 HEX、帧日志运行时开关（/api/debug/logframes）、命令耗时统计（/api/debug latency）、Web 面板修复（死按钮→客户端"异常"关键词过滤、.paused 接线、seq 增量追加）+ 级别过滤按钮组。**顺手修了真实缺陷：PLC 断线时发命令会崩 HSMS 连接 → try-catch 按失败处理（Scenario30 回归）**
**B. 预演调试台**：Simulator 增加 plc/mcs REPL 模式（管道脚本驱动，demo 保留）；PlcSim 注入可观测；McsSim 断连注入；deploy/场景/ 4 个故障场景脚本（断线重连/回显丢失/命令挂起/告警风暴）全 PASS
**C. 联调流程**：deploy/现场使用手册.md 重写（§0 预演闸门/检查项结果栏/§4 集成场景/§5 故障排查表/§6 证据留存+回填闭环）；联调预演.sh 同步手册到分发包；开发文档 §6 技术栈更新 + §5 回填闭环说明
**经验**：①S2F41 HCACK=4 是异步执行确认（设计行为），成败看 S6F11 事件/9001 告警——场景断言别用 HCACK 区分 ②Windows 控制台重定向日志是 GBK，脚本 grep 断言用 ASCII 锚点（ip:port/HEX/CEID）③场景脚本时序注意 publish 时长漂移，plc 保持窗口要覆盖全流程 ④**发布前必须停 BufferC + 清空发布目录**（Windows 文件锁静默导致 exe 覆盖失败跑旧代码；net10 历史残留混入 net6 产物会让 WebUi 崩溃——已修进场景脚本 publish_all）
**验证**：0 警告 + **55/55 测试全绿**（新增 4 个）+ 4 场景全 PASS + 联调预演 PASS + 本机全功能验证（日志分级/MODBUS 帧/帧开关/命令成功失败路径/统计口径/mcs REPL/Web 新 UI）——期间**修统计口径缺陷**：CommandChannel 传输异常不计 CommandFailCount（catch 后按尝试失败处理，最终计入）

## 2026-08-11：现场 OS 确认 → .NET 10 降级 .NET 6

现场服务器确认是 **CentOS Linux 7.8（glibc 2.17）**。.NET 8+ 最低要 glibc 2.23（.NET 9/10 更高），只有 **.NET 6 自包含**能跑 CentOS 7。已完成：
- 全工程 net10.0 → **net6.0**，包版本同步降（Microsoft.Data.Sqlite 6.0.36、SQLitePCLRaw.bundle_e_sqlite3 2.1.10、Microsoft.NET.Test.Sdk 17.8.0、xunit.runner.visualstudio 2.8.2）；删掉未使用的 Secs4Net/NModbus 残留包引用（代码均为自研实现）
- WebUi.cs 适配 net6（CreateSlimBuilder→CreateBuilder、Results.*→HttpContext 直写）
- **踩坑：SDK 10.0.301 + net6.0 classlib 包 dll 不拷入输出**（Exe 正常）——冲突解析链路不产出拷贝项；解法 `CopyLocalLockFileAssemblies=true`（三个 classlib 工程都加了）。17.12+ 的 Test SDK 还有 testhost 拷贝断裂问题 → 降到 17.8.0
- ✅ 0 警告编译 + **51/51 测试全绿**（连跑 2 遍）；linux-x64 自包含产物已重新发布（bufferc-linux-x64.tgz 已更新，含 libe_sqlite3.so）
- **glibc 符号验证（本机 WSL objdump -T 实测）**：初版产物 libe_sqlite3.so 依赖 GLIBC_2.28（SQLitePCLRaw 2.1.10）→ **2.1.5 起基线抬升，降到 2.1.4**（≤2.14）；终检全部产物符号 ≤ 2.17（最高 2.16）✓。教训：**manylinux2014 假设不可靠，原生库 glibc 兼容要用 objdump -T 实测**
- **ICU 坑**：.NET 6.0.36 runtime 包无 ICU 本体 → 启动 FailFast "Couldn't find a valid ICU package"；解法 **InvariantGlobalization=true**（Host csproj 固定）。代码无文化敏感操作，零副作用
- **本机 CentOS7 WSL 测试环境已搭建**（真 2.17）：`wsl --import CentOS7 C:\WSL\CentOS7 <centos:7 rootfs>`（rootfs 来自 quay.io centos/centos:7，Docker Hub 被墙不可用）；完整验证通过（启动/HSMS 5000/Web 8080/api/status）。日常：`wsl -d CentOS7` → `~/bufferc` 解包运行
- **联调预演环境（2026-08-11）**：`bash deploy/联调预演.sh` 一条命令全自动——Windows 侧 PlcSim/McsSim 仿真（tests/Simulator 新工程）+ CentOS7 WSL 侧 BufferC，验证 PLC↔BufferC↔MCS 全链路（注入 AGV 路径 0→2→1 → S1F3 查询 2 载具 → 取出 1→0 → 查剩 1 → S6F11×2）。已跑通 PASS（退出码 0）
  - 坑：①WSL 内 nohup 后台化会被实例关闭连带杀掉 → wsl.exe 本体放 Windows 后台（会话保持）②PLC 仿真要绑 Any（PlcSim 默认 Loopback）③货物放入必须走 0→2→1，状态 2 只是中间态不触发库存 ④WSL→Windows 入站首次连接可能被防火墙瞬态拒，重跑即通；需要时 netsh 加规则（需管理员）
- 顺手修了 EventStorm 压测的 flaky（固定 30ms delay 赌 poller 周期 → 改为显式等待 10s 重试，同 AlarmStorm 模式；教训②的漏网之鱼）
- 部署说明改 CentOS 7 + firewalld（原 ufw 是 Ubuntu 专属）
- ⚠️ 遗留：CentOS 7 本身 2024-06 已 EOL，.NET 6 已 EOL——长期建议推动现场换 Rocky 9 / Ubuntu 24.04（届时可回 .NET 10）

## 当前状态：可跑可测可监控的核心已交付 ✅

| 维度 | 状态 |
|---|---|
| 开发文档 | ✅ Obsidian Vault `公司项目/B16BufferC_V2.0/BufferC开发文档.md`（唯一模型，8 章） |
| 编译 | ✅ 0 警告（CI 用 -warnaserror 门禁） |
| 测试 | ✅ **51/51 全绿**（编解码/状态机/场景回归 26/超时补偿/15 台并发/压力/配置校验/Web API） |
| CI | ✅ GitHub Actions 每推全绿（`.github/workflows/ci.yml`） |
| 产物 | ✅ `publish/win-x64`（联调）+ `publish/linux-x64`（部署）+ 现场手册 + 启停脚本 |

## 已实现功能

- **PLC 侧**：15 连接并发轮询、按站口变化读 ID、统一命令通道（回显匹配/5s 超时重试）、扫码握手 340、断线重连+重同步（不补报）
- **MCS 侧**：HSMS 建连全套、S1F3/4（5 SVID，l4/l2 结构开关）、S2F41 命令、S6F11 事件全套（SC 告警去重）、S5F1、S9、T3
- **业务**：事件引擎、库存台账（SQLite 持久化）、命令悬空补偿（C1：失败→S5F1 9001+记录）
- **接口**：AGVC HTTP 骨架（/api/agvc/delivery，与 S2F41 共用命令路径）
- **Web 监控界面**（webPort 8080）：MCS/PLC 连接健康、240 站口状态（有货/下线/告警/截断提示）、寄存器视图（0~49+306~340 对照 V8）、事件流/命令历史/告警历史、实时日志面板（过滤/暂停）、手动命令二次确认
- **部署**：Linux systemd 服务文件 + 部署说明；Windows 前台/后台/停止三个脚本

## 明天（8/4）联调计划

1. 拷 `publish\win-x64` 到笔记本，改 `config.json`（PLC IP/unitId/byteOrder）
2. 启动 → Web 界面确认 15 台 PLC 在线徽标 → 寄存器视图对照 V8 逐项核对
3. 按 `现场使用手册.md` 检查清单过 PLC 6 项 / MCS 7 项
4. **现场确认项**（回来回填开发文档）：字节序、unit_id、S1F4 结构（l4/l2）、S2F41 参数名、告警码表、A1 AGV/人工区分、B2 CarrierID 64/32

## 待确认项（开发文档 §5，剩余 14 项）

- ⚠️ A1 AGV/人工事件区分（推荐方案 B 操作类型寄存器）
- ⚠️ B2 CarrierID 64 vs 32 字符
- B3 消息实现范围、B5 E88 裁剪确认、D1 AGVC 字段规范
- 现场项：字节序/unit_id/S1F4 结构/告警码表

## 经验教训（写给自己）

- 跑测试前先停 BufferC.Host（占 5000 端口）——已踩 5 次
- CI 比本地慢一个数量级：测试用"显式等待+重试"代替"固定 delay"（已全面整改）
- 发布产物要检查配置/资源是否被 csproj 复制（config.json 漏过一次）
